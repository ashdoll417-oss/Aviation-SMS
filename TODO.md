- [ ] Update `safety_assurance_post()` in `app.py` to be bulletproof:
  - [ ] Replace audit_date + next_audit_date parsing with your exact robust try/except structure
  - [ ] Commit the SafetyAssurance record immediately before any email logic
  - [ ] Use professional email template and fallback `audit_id_val = assurance.id if assurance.id else "PENDING"` for links
  - [ ] Wrap entire email dispatch block in `try/except Exception as mail_err:` and always redirect/return cleanly even on mail failures
- [ ] Re-run basic sanity check by locating the edited block and ensuring no syntax errors
